# Resources

A personal collection of books, job boards, links, video channels, and people worth following.

**Live site: <https://samehrlich.github.io>**

More of my work: <https://github.com/SamEhrlich>

## Local development

```
npm install
npm run dev
```

React + Vite. `npm run build` outputs to `dist/`; pushes to `main` deploy automatically.
